<div class="admin">
  <h1 class="latest-rants-header">Rants</h1>
  <div class="admin-links">
    <%= link_to "All", admin_rants_path %>
    <%= link_to "Spam", admin_spams_path %>
  </div>


  <table class="table">
    <thead>
    <tr>
      <th>Name</th>
      <th>E-mail</th>
      <th>Title</th>
      <th>Action</th>
    </tr>
    </thead>
    <tbody>
    <% @spams.each do |spam| %>
      <tr>
        <td><%= "#{spam.rant.user.first_name} "  "#{spam.rant.user.last_name}"  %></td>
        <td><%= spam.rant.user.email %></td>
        <td><%= spam.rant.description %></td>
        <td><%= link_to "Not Spam", admin_spam_path(spam.rant), {method: :delete} %> <%= link_to "Delete", admin_rant_path(spam.rant), {method: :delete} %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>